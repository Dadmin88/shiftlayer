{
  "name": "shiftlayer",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first browser extension that lets you move page elements and keep them there.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "lint": "node scripts/lint.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
